<div class="header">
  <div class="container">
    <img src="http://s3.amazonaws.com/codecademy-content/courses/learn-rails/img/logo-1tm.svg" width="80">
    <h1></h1>
  </div>
</div>

<div class="destination">
  <div class="container">
        <%= image_tag @recipe.image %>
        <h1><%= @recipe.name %></h1>
        <p><%= @recipe.ingredients %></p>
        <p><%= @recipe.instructions %></p>  
   
        <% if current_user && current_user.editor? || current_user.admin? %>
        <p><%= link_to "Edit", edit_recipe_path(@recipe) %></p>
        <% end %> 

       
       <% if current_user && current_user.admin? %>
        <p><%= link_to "Delete", recipe, method: :delete, :class => "btn btn-default" %><p>
        <% end %> 

  </div>
</div>

<!-- 
issue; 

delete method not working -->